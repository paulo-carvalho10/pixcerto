{
  "name": "pixcerto",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Gerador e decodificador de cobranca Pix (BR Code) que roda inteiramente no navegador.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.9",
    "vitest": "^3.2.4"
  }
}
